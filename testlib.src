START	= $2000
	.include "libflash.inc"
	.include "cio.inc"

	.bank

	* = START

	LDA #$80
	STA 106
	JSR GR0

	JSR FINIT
	JSR FENABLE
	JSR FDETECT
	JSR FDISABLE
	LDA FTYPE
	JSR PUTHEX
X	JMP X


	RTS

.IF .DEF BERND
	.include "libflash-mega512.src"
.else
	.include "libflash-atarimax8.src"
.endif
	.include "iohelp.src"

	.bank
	* = $2E0
	.WORD START
